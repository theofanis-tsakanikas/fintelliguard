# agents/langgraph/ — self-healing

**LangGraph** Supervisor + Medic that watch the pipeline, detect failures, and attempt
guided recovery. Traced end-to-end in **LangSmith**. Operational resilience layer —
separate from the decisioning funnel.
