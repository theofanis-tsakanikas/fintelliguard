# Local secret-scanning guardrail. Enable once with:  pre-commit install
# (pip install pre-commit). gitleaks then scans staged changes on every commit.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
