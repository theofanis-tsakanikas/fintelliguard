"""Smoke test so the suite runs green before any application code lands.

Replace/extend as each layer (pipelines, ml, agents, simulator) gains real tests.
"""


def test_repo_is_alive():
    assert True
