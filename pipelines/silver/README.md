# pipelines/silver/

**Silver** DLT tables — cleaned, deduplicated, conformed transactions with enforced
expectations (data-quality constraints). The trustworthy single source feeding gold.
Tables: `silver.<name>`.
